##
##

#options(repos=c(CRAN="http://ftp.heanet.ie/mirrors/cran.r-project.org"))
options(repos=c(CRAN="http://ftp.sunet.se/pub/lang/CRAN/"))

  
#Sys.setenv (R_HISTFILE="~/.Rhistory")
.First <- function () {
  if(interactive()) try(loadhistory("~/.Rhistory"))
}

.Last <- function () {
  if(interactive()) try(savehistory("~/.Rhistory"))
}
